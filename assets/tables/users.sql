CREATE TABLE users (
    id int,
    name varchar(255),
    email varchar(255),
    telefone varchar(255),
    cpf varchar(255),
    status tinyint(1),
    created datetime(),
    modified datetime(),
);